# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
.env
.coverage

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project specific
data/
config/secrets.yaml
config/local.yaml
*.log
logs/

# GTO API file cache (dir in main repo, symlink in worktrees — no trailing
# slash so both forms are ignored)
.gto_cache

# Telegram bot token
bot_token.txt

# Browser automation
*.png
*.jpg
downloads/
# Allow snapshot test images
!tests/snapshots/**/*.jpeg
!tests/snapshots/**/*.png
# Allow title-OCR regression fixtures (tiny, faithful title bands)
!tests/fixtures/*.png
# Snapshot GTO outputs are machine-local (depend on local .gto_cache)
tests/snapshots/*/gto_text.txt
tests/snapshots/*/gto_compact.txt
tests/snapshots/gto_cache/
.browser-profile/
# Legacy credential-file tombstone: never allow an old local secret into git.
.tokens.json
scripts/_tmp.py


# OCR template raw crops
scripts/ocr/templates/raw/
!scripts/ocr/templates/dealer_button.png
.game_modes_cache.json

# Card classifier training crops (rebuildable via extract_crops.py)
data/cards/


.claude/scheduled_tasks.lock
data/effbb_cache/
scripts/_tmp*.py

# Phase 1 ledger
data/gtow_raw/
data/scorecards/
.gtow_client_id
dist/

# Extension icons are shipped assets (built from the bot TG avatar)
!chrome-extension/icons/*.png

# Local agent and one-off operational artifacts
.gstack/
scripts/_delete_*_backup*.json
scripts/_rerun_*_backup.json
scripts/_rerun_*_result.json
scripts/_rerun_*_summary.txt
scripts/_h*_db_backup.json
